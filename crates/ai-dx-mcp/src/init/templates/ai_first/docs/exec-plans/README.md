# Execution plans

Store bounded execution plans here. Use `TEMPLATE.md` as the canonical starting point.

Each plan should capture:
- goal
- non-goals
- acceptance checks
- rollback
- linked PR or issue when available
